# BetterTouchTool (optional)

Create two named triggers:
- `Atlas Copy Chat` -> execute script that invokes snippet `copy-chat`.
- `Atlas Download Chat` -> execute script that invokes snippet `download-chat`.

BTT stays a wrapper only; extraction remains in shared `packages/core`.
