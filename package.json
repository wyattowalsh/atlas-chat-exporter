{
  "name": "atlas-chat-exporter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "verify-fixtures": "node scripts/verify-fixtures.js",
    "ci": "npm run verify-fixtures && npm run test"
  }
}
