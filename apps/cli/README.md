# CLI

Commands:
- `atlas-export export [--json] [--out=path]`
- `atlas-export copy`
- `atlas-export download [--json] [--out=path]`

Stable exit codes are exported by `packages/core` (`EXIT_CODES`).
Browser targeting concerns are isolated in `src/browser-target.js`.
